<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="CardView.Own">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginLeft">@dimen/card_view_marginHorizontal</item>
        <item name="android:layout_marginRight">@dimen/card_view_marginHorizontal</item>
        <item name="android:layout_marginTop">@dimen/card_view_marginVertical</item>
        <item name="android:layout_marginBottom">@dimen/card_view_marginVertical</item>
        <item name="cardBackgroundColor">@color/card_background</item>
    </style>

    <style name="TextViewInCard">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:fontFamily">roboto medium</item>
        <item name="android:textSize">@dimen/card_text_view_size</item>
        <item name="android:paddingLeft">@dimen/card_view_paddingHorizontal</item>
        <item name="android:paddingRight">@dimen/card_view_paddingHorizontal</item>
        <item name="android:layout_marginBottom">@dimen/card_id_text_margin_bottom</item>
    </style>

</resources>
